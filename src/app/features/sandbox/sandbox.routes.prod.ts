import { Routes } from '@angular/router';

/**
 * Producción: el banco de pruebas visual (`/sandbox`) no se registra ni se
 * empaqueta. Este archivo reemplaza a `sandbox.routes.ts` en el build de
 * producción (ver `fileReplacements` en `angular.json`).
 */
export const sandboxRoutes: Routes = [];
